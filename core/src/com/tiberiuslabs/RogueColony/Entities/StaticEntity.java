package com.tiberiuslabs.RogueColony.Entities;

/**
 * Interface for interacting with non-NPC/PlayerChar entities
 */
public interface StaticEntity {
    // have the entity render itself in place
    void render();
}
